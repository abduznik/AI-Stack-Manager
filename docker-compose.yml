version: '3.8'

services:
  ai-manager:
    build: .
    ports:
      - "8090:8090"
    volumes:
      - ./app/workspace:/app/workspace
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - GH_TOKEN=${GH_TOKEN}
    restart: unless-stopped